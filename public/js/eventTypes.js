// Shared across the Log Event form and Areas settings, so an area's
// "populates under" checkboxes and the event-type toggle buttons always
// agree on the same set of keys/labels.
export const EVENT_TYPE_LABELS = {
  yardwork: "Yard Work",
  extra_yardwork: "Extra Yard Work",
  chemical: "Chemical Application",
};

export const EVENT_TYPE_KEYS = Object.keys(EVENT_TYPE_LABELS);
